import { defineConfig } from 'astro/config';
// site/base are updated in Task 19 once the GitHub repo URL is known.
export default defineConfig({
  site: 'https://USERNAME.github.io',
  base: '/portfolio',
});